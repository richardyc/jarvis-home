org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# JDK 17 is required. If it isn't your default, set org.gradle.java.home in
# ~/.gradle/gradle.properties (user-level, never committed) rather than here —
# an absolute path in this file leaks your username and breaks every other clone.
